import { Workflow } from "lucide-react";
import { PlaceholderPage } from "../../components/layout/PlaceholderPage";

export default function HowItWorksPage() {
  return (
    <PlaceholderPage
      icon={Workflow}
      title="How It Works"
      description="The 8-step flow — Register, AI Conversation, Documents, AI Processing, AI Summary, Doctor Review, Final Report, Save & Access — will be built in a later part."
    />
  );
}

import { CheckCircle2 } from "lucide-react";
import { PlaceholderPage } from "../../components/layout/PlaceholderPage";

export default function OnboardingSuccessPage() {
  return (
    <PlaceholderPage
      icon={CheckCircle2}
      title="You're all set"
      description="The onboarding success screen — showing your new Health ID card and QR code — will be built alongside registration in a future part."
    />
  );
}
